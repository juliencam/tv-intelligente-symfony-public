    <nav class="navbar navbar-expand-lg navbar-light bg-light sticky-top">
        <div class="container-fluid" >
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>

            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="nav flex-column">

                    {% for tag in tags %}
                    <li class="nav-item">
                        <a class="nav-link nav_a_text text-break
                        {% if (app.request.get('_route') in ['tag_post']) and (tag.id in [app.request.attributes.get('id')])  %}
                            {{ 'nav_a_active' }}
                        {% endif %}"
                        href="{{ path('tag_post', {'id':tag.id}) }}">
                        {{ tag.name }}
                        </a>
                    </li>
                    {% endfor %}
                    {% if (app.request.attributes.get("_route") not in ["home", "article_category",'article_tag', 'tag']) and (boolSortByDate) %}

                    <div class="col-lg-2">
                        <div class="dropdown">
                          <button class="btn form_button_background-color dropdown-toggle form_element_border" type="button" id="dropdownMenuButton1" 
                                  data-bs-toggle="dropdown" aria-expanded="false">
                            Filtrer par date
                          </button>
                          <div class="dropdown-menu" aria-labelledby="dropdownMenuButton1">
            
                            {{ form_start(filter) }}
            
            
                                      {{ form_row(filter.date) }}
            
                                    <div class="d-flex justify-content-center">
                                      {{ form_row(filter.search, {'attr': {'class': 'form_button_background-color'}}) }} 
                                    </div>
            
            
            
                            {{ form_end(filter) }}
            
                          </div>
                        </div>
                    </div>
            
                    {% endif %}
                </ul>
 
            </div>
        </div>
    </nav>



